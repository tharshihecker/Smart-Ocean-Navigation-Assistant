// Central icon exports. If a specific icon (e.g., RouteIcon) doesn't exist in heroicons,
// we alias it to a close alternative to avoid runtime/import errors.
export { MapIcon as RouteIcon } from '@heroicons/react/24/outline';
